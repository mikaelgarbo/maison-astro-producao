---
const { title, description, url, image, pubDate, faqs } = Astro.props;

const articleSchema = {
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": title,
  "description": description,
  "image": image,
  "author": {
    "@type": "Person",
    "name": "Mikael Garbo",
    "jobTitle": "Editor-in-Chief & Architectural Analyst",
    "url": "https://maisoninteriorshome.com/about"
  },
  "publisher": {
    "@type": "Organization",
    "name": "Maison Interiors",
    "logo": {
      "@type": "ImageObject",
      "url": "https://maisoninteriorshome.com/logo.png"
    }
  },
  "datePublished": pubDate,
  "dateModified": pubDate,
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": url
  }
};

const faqSchema = faqs && faqs.length > 0 ? {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqs.map((faq: any) => ({
    "@type": "Question",
    "name": faq.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": faq.answer
    }
  }))
} : null;
---

<script type="application/ld+json" set:html={JSON.stringify(articleSchema)} is:inline></script>
{faqSchema && <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} is:inline></script>}